import {TableDice,SecondTable} from "./tableClass"

export class TableDiceContainer {
    container:TableDice[]
    constructor(tableDice:TableDice[]){
        this.container =  tableDice
    }
    
}